<% if policy(@courses).new? %>
  <%= render("layouts/button-new-fly", goto: new_course_path) %>
<% end %>

<div class="section container">
  <div class="row center">
    <h5 class="header">"Belajar Apa Hari Ini?"</h5>
    <div class="divider"></div>
  </div>
  <div class="row center-cols center-align">
    <%= render('courses/layouts/main', courses: @courses) %>
  </div>
</div>

